#include <bits/stdc++.h>

using namespace std;

const int64_t MAX = INT64_MAX;
const int64_t MIN = INT64_MIN;

typedef long long ll;
typedef unsigned long long ull;
typedef vector<int> vi;
typedef pair<int, int> ii;
typedef vector<ii> vii;

void solve(){
	int n, m, l;
	cin >> n >> m >> l;
	
	vii obs(n);
	for(auto &a: obs) cin >> a.first >> a.second;

	vii powup(m);
	for(auto &a: powup) cin >> a.first >> a.second;

	vii mypowup;
	for(int i=0)

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);

	int t;
	cin >> t;
	while(t--){
		solve();
	}

    return 0;
}
